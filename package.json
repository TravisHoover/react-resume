{
  "name": "react-resume",
  "homepage": "http://travishoover.net",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "gh-pages": "~1.1.0",
    "react": "^16.2.0",
    "react-dom": "^16.2.1",
    "react-scripts": "1.1.0"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "deploy": "npm run build && gh-pages -d build",
    "eject": "react-scripts eject"
  }
}
