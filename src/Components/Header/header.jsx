import React from 'react';
import NameAndTitle from '../NameAndTitle/nameAndTitle';
import data from '../../data';

const Header = () => (
		<NameAndTitle NameAndTitle={data.NameAndTitle}/>
);

export default Header;